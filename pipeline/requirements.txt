# PyPI-Paketname ist "osmium" (Import bleibt `import osmium`); "pyosmium"
# existiert auf PyPI nicht mehr. Ziel-API ist osmium 4.x (TagList ohne keys()).
osmium>=4,<5
SQLAlchemy>=2.0,<3
psycopg[binary]>=3.1,<4
geopandas>=0.14
pyarrow>=15
shapely>=2.0
requests>=2.31
